[Unit]
Description=GeoProxy mesh sync (register + pull peers)
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
ExecStart=+__BIN__ mesh sync-master
NoNewPrivileges=true
ProtectSystem=strict
ReadWritePaths=__ETC_DIR__ __LOG_DIR__
ProtectHome=true
PrivateTmp=true
